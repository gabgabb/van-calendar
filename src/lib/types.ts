export type Station = {
    id: string;
    name: string;
};

export type Booking = {
    id: string;
    startDate: string;
    endDate: string;
    customerName: string;
    pickupLocation: string;
};
