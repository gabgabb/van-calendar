services:
    nextjs:
        build:
            context: .
            dockerfile: ./docker/Dockerfile
        ports:
            -
        volumes:
            - .:/app
        environment:
            NODE_ENV: development
        restart: always
